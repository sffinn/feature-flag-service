PORT=8080
DATABASE_URL=postgres://flags:flags@localhost:5432/flags?sslmode=disable
REDIS_URL=redis://localhost:6379/0
CACHE_TTL_SECONDS=60

# Host-side ports published by docker-compose. Override if these collide
# with services already running locally.
POSTGRES_PORT=5432
VALKEY_PORT=6379
API_PORT=8080
